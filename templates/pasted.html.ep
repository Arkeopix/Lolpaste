% layout 'default';
  %= tag div => id => pasted => begin
  %= tag div => id => title => 'Title: ' . $file_info->{title}
  %= tag div => id => type => 'Type: ' . $file_info->{type}
  Content:
  %= tag 'pre' => id => content => <%= $file_info->{text} 
  %= end
